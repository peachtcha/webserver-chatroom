{
  "name": "sim-webserver",
  "version": "1.0.0",
  "description": "简单的HTTP和WebSocket服务器，支持聊天室功能",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "websocket",
    "chat",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
